# Traefik / Let's Encrypt
ACME_EMAIL=tu-email@planificador.app

# MySQL
DB_ROOT_PASSWORD=cambia_esto_root
DB_USER=planificador
DB_PASSWORD=cambia_esto_user
DB_NAME=invitaciones_saas

# JWT (genera con: openssl rand -hex 64)
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=

# Solo en el primer deploy para sembrar datos base
SEED_ON_BOOT=true
